/**
 * 管理向け API の境界をまたぐデータ構造。入力は request、出力は response に置き、いずれも record にする。
 *
 * <p>
 * 利用者向けの DTO と共有しないこと。管理画面が見たい項目（内部 ID、無効化フラグ、更新日時など）は
 * 利用者に返してよいものと一致しない。同じ形になったとしても、片方の変更でもう片方の API が 変わらないよう別に定義する。
 *
 * <p>
 * 現時点では空。
 */
package com.example.spacereserve.admin.dto;
